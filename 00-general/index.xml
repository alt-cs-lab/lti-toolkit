<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Documentation :: LTI Toolkit</title>
    <link>https://alt.cs.ksu.edu/lti-toolkit/00-general/index.html</link>
    <description>General Documentation for the LTI Toolkit&#xA;Examples Database Schema Changelog</description>
    <generator>Hugo</generator>
    <language>en-us</language>
    <atom:link href="https://alt.cs.ksu.edu/lti-toolkit/00-general/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Examples</title>
      <link>https://alt.cs.ksu.edu/lti-toolkit/00-general/00-examples/index.html</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://alt.cs.ksu.edu/lti-toolkit/00-general/00-examples/index.html</guid>
      <description>LTI Toolkit Example Project Documentation&#xA;LTI Provider LTI Consumer</description>
    </item>
    <item>
      <title>Database Schema</title>
      <link>https://alt.cs.ksu.edu/lti-toolkit/00-general/01-database/index.html</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://alt.cs.ksu.edu/lti-toolkit/00-general/01-database/index.html</guid>
      <description>System Managed Tables lti_migrations This is the migrations table managed by Umzug. It tracks any changes that must be made to the database schema as future versions are released.&#xA;LTI Consumers lti_consumers This table stores data about LTI Consumers connected to this system.&#xA;id - autogenerated database ID field name - user-friendly name of the LTI Consumer lti13 - boolean value representing if the LTI Consumer supports LTI 1.3 connections key - Both autogenerated internal key used to identify the LTI Consumer This key is used in lieu of the database ID to prevent guessing attacks against sequential values. The key is generated using nanoid client_id - LTI 1.3 client ID provided by the LTI Consumer platform_id - LTI 1.3 platform ID of the LTI Consumer deployment_id - LTI 1.3 deployment ID from the LTI Consumer keyset_url - LTI 1.3 JWKS keyset URL for the LTI Consumer token_url - LTI 1.3 Token URL for the LTI Consumer auth_url - LTI 1.3 Authentication URL for the LTI Consumer tc_product - Both Auto Product Information for the LTI Consumer tc_version - Both Auto Version of the LTI Consumer tc_guid - Both Auto GUID of the LTI Consumer tc_name - Both Auto Name of the LTI Consumer createdAt - Auto creation timestamp updatedAt - Auto last updated timestamp lti_consumer_keys This table stores the secrets associated with an LTI Consumer. It is stored in a separate table to prevent accidental modification through the Consumer model directly.</description>
    </item>
    <item>
      <title>Changelog</title>
      <link>https://alt.cs.ksu.edu/lti-toolkit/00-general/02-changelog/index.html</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://alt.cs.ksu.edu/lti-toolkit/00-general/02-changelog/index.html</guid>
      <description>All notable changes to this project will be documented here. This project follows Semantic Versioning and the format is based on Keep a Changelog.&#xA;[1.2.0] — 2026-08-08 Added createLineItem() and updateLineItem() — new LTIProviderController methods to create and update LTI 1.3 AGS line items on a Tool Consumer. Gated behind a new provider.enableLineItemManagement config flag (default false), which also requests the https://purl.imsglobal.org/spec/lti-ags/scope/lineitem scope (in addition to lineitem.readonly, not instead of it) during LTI 1.3 Dynamic Registration startDateTime/endDateTime support for line items — createLineItem()/updateLineItem() accept optional startDateTime/endDateTime fields and validate them as ISO 8601 date-time strings with a mandatory timezone designator, per the AGS spec Grade any student on any line item (example Provider app) — the example Provider app’s instructor and student views now support posting/submitting grades against any AGS line item in the course, not just the one tied to the current launch. Instructors pick from a course-level roster of students who have launched the tool; students get a line item selector on their own grade form Fixed HTTP error visibility — failed requests to the LMS (access token, line item, results, grade passback, and Dynamic Registration requests) now log the platform’s actual error response body (e.g. Canvas’s { error, error_description }) at the error log level, instead of only a generic message that discarded the useful detail Line item date/time inputs now use the browser’s local timezone (example Provider app) — the create/update line item forms previously treated entered times as if they were already UTC; they’re now correctly converted from the instructor’s browser-local time Student grade score local-cache key mismatch (example Provider app) — the student grade form was looking up its local display cache with the wrong key, so a student’s own submitted score never updated in the local UI (the grade was still posted to the LMS correctly) [1.1.2] — 2026-08-06 Changed Dynamic Registration scope request — the tool now requests lineitem.readonly and result.readonly AGS scopes in addition to score during LTI 1.3 Dynamic Registration, so Tool Providers can read line items and results from Canvas without a separate manual scope grant Fixed AGS request URL normalization — postGrade(), getLineItem(), getLineItems(), and getResults() now force https:// on platform-supplied AGS endpoint URLs before requesting. This works around platforms (observed on self-hosted Canvas) that report some AGS claim URLs with an http:// scheme, which previously caused the Authorization header to be dropped on the resulting redirect and requests to fail with 401 errors [1.1.0] — 2026-06-18 Added LTI 1.3 Tool Consumer — the library can now initiate LTI 1.3 launches to registered Tool Providers using the OIDC login flow (generateLTI13LaunchFormData) LTI 1.3 Tool Consumer grade passback — Tool Providers can now post grades back via AGS; the consumer router handles score, line item, and results endpoints LTI 1.3 Dynamic Registration as a consumer — register remote Tool Providers by sending them an OpenID Configuration URL (lti13dynamicregistration) LTI 1.3 Deep Linking as a consumer — initiate Deep Linking requests to Tool Providers (generateLTI13DeepLinkFormData) and receive content items back via the handleDeeplink consumer config callback postGrade() activity and grading progress — LTIProviderController.postGrade() now accepts optional activityProgress and gradingProgress parameters validated against the LTI AGS spec; defaults are &#34;Submitted&#34; and &#34;FullyGraded&#34; getLineItems() — new LTIProviderController method to fetch the full AGS line items collection from a consumer; the AGS JWT claim now includes the lineitems collection URL alongside lineitem readGrade() and deleteGrade() — new LTIProviderController methods for LTI 1.0 Basic Outcomes readResult and deleteResult operations readProviderGrade and deleteProviderGrade callbacks — optional consumer config callbacks to serve readResult/deleteResult Basic Outcomes requests from Tool Providers getResults() — new LTIProviderController method to fetch AGS submission results from a consumer getProviderLineItem and getProviderLineItems callbacks — optional consumer config callbacks to serve AGS line item requests from Tool Providers At-rest encryption — all stored secrets and RSA private keys are now encrypted using AES-256-GCM; requires a new encryption_key config option (64-character hex string) External database support — pass a pre-configured Sequelize instance as database in the config to use any Sequelize-supported database instead of the built-in SQLite TypeScript type declarations — index.d.ts now includes typed interfaces for all four controllers (ConsumerRegistryController, ProviderRegistryController, LTIConsumerController, LTIProviderController) and their return shapes (ConsumerInstance, ProviderInstance, ConsumerKeyData, ProviderKeyData, LaunchFormData, etc.) Callback return validation — all user-provided callbacks are now validated at runtime; missing or wrong-typed fields produce descriptive errors rather than silent failures downstream Changed Controller accessor paths ⚠️ Breaking — lti.controllers.provider (was lti.controllers.lti.provider) and lti.controllers.consumer (was lti.controllers.lti.consumer); the intermediate .lti segment has been removed AGS token scopes — the token endpoint now advertises all three scopes this library implements: score, lineitem.readonly, and result.readonly (previously only score was listed) Dynamic Registration response — the registration response is now filtered to the fields required by the LTI 1.3 spec, dropping extra fields that caused errors with some Tool Providers OAuth 1.0 signature base URI — the base URI now reads X-Forwarded-Proto and X-Forwarded-Host headers so signature verification works correctly behind a reverse proxy Dynamic Registration URL validation — the registration endpoint URL is now validated by comparing URL origins rather than a startsWith prefix check, preventing bypass with crafted URLs Removed provider.key / provider.secret config shortcut ⚠️ Breaking — this option previously created a single LTI 1.0 consumer at startup but also destroyed all other consumers on every restart. It has been removed. Use consumerRegistry.createConsumer() in application startup code to seed a default consumer instead. Fixed LTI 1.3 launches now validate deployment_id against the stored consumer record; mismatches are rejected with a clear error LTI 1.3 login state is no longer destroyed before deployment_id validation, preventing a race condition that could cause valid launches to fail LTI 1.0 XML configuration now properly XML-escapes custom parameter keys and values The redirect_urls provider field was stored under an incorrect key in v1.0; it is now stored correctly Security All shared secrets and RSA private keys are now encrypted at rest using AES-256-GCM with a key derived from the new required encryption_key config option. Existing plaintext values in v1.0 databases must be re-entered after upgrading. LTI 1.3 deployment_id is now validated on every launch, preventing launches from unregistered deployments. [1.0.0] — 2025-02-04 Initial release.</description>
    </item>
  </channel>
</rss>